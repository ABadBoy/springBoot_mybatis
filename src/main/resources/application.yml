server:
  port: 9090

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/aaa
    driver-class-name: com.mysql.jdbc.Driver
  mvc:
    view:
      prefix: /WEB-INF/web/
      suffix: .html

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.badboy.model

app:
  id: helloWorld

apollo:
  meta: http://localhost:8080

